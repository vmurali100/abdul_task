import { CustomdatesecDirective } from './customdatesec.directive';

describe('CustomdatesecDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdatesecDirective();
    expect(directive).toBeTruthy();
  });
});
